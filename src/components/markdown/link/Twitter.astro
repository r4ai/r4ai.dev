---
import { Tweet, TweetNotFound } from "react-tweet"
import { getTweetId } from "./Twitter"

type Props = {
  url: URL
}
const { url } = Astro.props

const tweetId = getTweetId(url)
---

{tweetId ? <Tweet id={tweetId} client:load /> : <TweetNotFound />}
